[project]
name = "payment-sync"
version = "1.0.0"
description = "Sync user info from Observation Sheet to Payment Details"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
]

[tool.uv]
dev-dependencies = []
